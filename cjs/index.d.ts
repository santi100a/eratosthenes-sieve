/**
 * Finds all prime numbers from 2 up to a given number `n`.
 *
 * @param n The upper limit of the range of numbers to check for primes.
 */
declare function sieve(n: number): number[];
export = sieve;
